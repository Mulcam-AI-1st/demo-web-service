<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.teamproject.demo.dao.MemberDAO">

   <select id="selectMember" parameterType="String"  resultType="com.teamproject.demo.vo.MemberVO">
   
             /* selectMember # member 에서 데이터 조회 */
            <![CDATA[
            SELECT  NAME, EMAIL, ID, PASSWORD,  TO_CHAR(SIGNUPDATE,'YYYY-MM-DD')  AS SIGNUPDATE
            FROM JTB_MEMBER
            WHERE id = #{id} and password = #{password} 
            ]]>
       </select>
       
</mapper>